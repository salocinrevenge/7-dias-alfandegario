Script install.sh install dependencies and run code

to run on desktop use on the main folder:

```uv run python main.py```

to run on web use on the main folder:

```uv run python -m pygbag --template web.tmpl .```

and acess 

```http://localhost:8000```
